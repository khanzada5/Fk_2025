let number = prompt ("Enter a number");

if ( number == 10 ) {
    document.body.innerHTML = "The number is exactly 10";
} 
else if (number > 10) {
    document.body.innerHTML = "The number is not 10";
}
else{
    document.body.innerHTML = "Pyarey Bhai num type karoo";
}